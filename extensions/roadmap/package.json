{
  "name": "@pi-atelier/roadmap",
  "version": "0.1.0",
  "description": "Epic→Story→Task 三层路线图管理",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup index.ts lib/*.ts --format esm,cjs --dts",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.5.0"
  }
}
